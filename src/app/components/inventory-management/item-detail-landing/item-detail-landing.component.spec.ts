import { ItemDetailLandingComponent } from './item-detail-landing.component';

describe('ItemDetailLandingComponent', () => {
  let component: ItemDetailLandingComponent;

  beforeEach(() => {
    component = new ItemDetailLandingComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
