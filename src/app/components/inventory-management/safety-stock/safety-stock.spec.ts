import { SafetyStockComponent } from './safety-stock';

describe('SafetyStockComponent', () => {
  let component: SafetyStockComponent;

  beforeEach(() => {
    component = new SafetyStockComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
