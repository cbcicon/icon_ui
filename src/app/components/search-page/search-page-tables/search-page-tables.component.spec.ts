import { SearchPageTablesComponent } from './search-page-tables.component';

describe('SearchPageTablesComponent', () => {
  let component: SearchPageTablesComponent;

  beforeEach(() => {
    component = new SearchPageTablesComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
