import { StudyDetailLandingComponent } from './study-detail-landing.component';

describe('ItemDetailLandingComponent', () => {
  let component: StudyDetailLandingComponent;

  beforeEach(() => {
    component = new StudyDetailLandingComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
