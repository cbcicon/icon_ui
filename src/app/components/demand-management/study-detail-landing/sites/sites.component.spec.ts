import { SitesComponent } from './sites.component';

describe('SitesComponent', () => {
  let component: SitesComponent;

  beforeEach(() => {
    component = new SitesComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
