import { CountriesComponent } from './countries.component';

describe('CountriesComponent', () => {
  let component: CountriesComponent;

  beforeEach(() => {
    component = new CountriesComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
