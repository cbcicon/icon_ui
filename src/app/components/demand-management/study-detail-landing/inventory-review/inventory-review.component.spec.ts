import { InventoryReviewComponent } from './inventory-review.component';

describe('InventoryReviewComponent', () => {
  let component: InventoryReviewComponent;

  beforeEach(() => {
    component = new InventoryReviewComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
