import { LiveStudyDetailLandingComponent } from "./live-study-detail-landing.component";

describe('ItemDetailLandingComponent', () => {
  let component: LiveStudyDetailLandingComponent;

  beforeEach(() => {
    component = new LiveStudyDetailLandingComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
