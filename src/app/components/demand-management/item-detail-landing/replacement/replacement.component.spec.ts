import { ReplacementComponent } from './replacement.component';

describe('ReplacementComponent', () => {
  let component: ReplacementComponent;

  beforeEach(() => {
    component = new ReplacementComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
