import { StockAndScrapComponent } from './stock-and-scrap.component';

describe('StockAndScrapComponent', () => {
  let component: StockAndScrapComponent;

  beforeEach(() => {
    component = new StockAndScrapComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
