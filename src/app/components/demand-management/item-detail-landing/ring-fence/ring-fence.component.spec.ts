import { RingFenceComponent } from './ring-fence.component';

describe('RingFenceComponent', () => {
  let component: RingFenceComponent;

  beforeEach(() => {
    component = new RingFenceComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
