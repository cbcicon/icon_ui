import { TableDataService } from './table-data.service';

describe('TableDataService', () => {
  const service: TableDataService = new TableDataService();

  it('should create', () => {
    expect(service).toBeTruthy();
  });
});
