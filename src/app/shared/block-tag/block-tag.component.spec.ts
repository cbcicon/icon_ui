import { BlockTagComponent } from './block-tag.component';

describe('BlockTagComponent', () => {
  let component: BlockTagComponent;

  beforeEach(() => {
    component = new BlockTagComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
