import { VerticalStepperComponent } from './vertical-stepper.component';

describe('VerticalStepperComponent', () => {
  let component: VerticalStepperComponent;

  beforeEach(() => {
    component = new VerticalStepperComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
