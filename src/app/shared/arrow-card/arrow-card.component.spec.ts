import { ArrowCardComponent } from './arrow-card.component';

describe('ArrowCardComponent', () => {
  let component: ArrowCardComponent;

  beforeEach(() => {
    component = new ArrowCardComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
