import { SmallCardComponent } from './small-card.component';

describe('SmallCardComponent', () => {
  let component: SmallCardComponent;

  beforeEach(() => {
    component = new SmallCardComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
